name: Stability-Test / Stability Test

on:
  push:
    branches: ["master"]
  pull_request:
    branches: ["master"]
  schedule:
    - cron: "0 4 * * *"

env:
  LOGGING_DEFAULT_SHOW_PREFIX: true
  LOGGING_DEFAULT_LOGGING_LEVEL: TRACE
  LOGGING_DEFAULT_SHOW_FORMATTING: true

jobs:
  build:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        runtime: [node, browser]
        target: [js, es6]

    steps:
      - name: Setup Maven Action
        uses: s4u/setup-maven-action@v1.8.0
        with:
          java-version: 8

      - uses: actions/checkout@v4

      - name: Link monorepo packages for Airship
        uses: ./.github/actions/setup-flat-monorepo

      - name: Download latest Airship
        uses: FlatLang/fetch-airship@master
        with:
          includePrerelease: true

      - uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Cache Maven dependencies
        uses: actions/cache@v4
        with:
          path: |
            ~/.m2/repository
          key: ${{ runner.os }}-build-${{ env.cache-name }}
          restore-keys: |
            ${{ runner.os }}-build-${{ env.cache-name }}-
            ${{ runner.os }}-build-
            ${{ runner.os }}-

      - name: Build Stability Test
        run: |
          node airship.js --use-links -f packages/Stability-Test install test --target ${{ matrix.target }} --runtime ${{ matrix.runtime }}

      - uses: actions/upload-artifact@v4
        if: failure()
        with:
          name: test.js
          path: packages/Stability-Test/dist/${{ matrix.target }}/test.js

      - name: Run Stability Test
        if: ${{ matrix.runtime == 'node' }}
        run: |
          node airship.js --use-links -f packages/Stability-Test run test --target ${{ matrix.target }}

      - name: Build Browser Stability Test
        if: ${{ matrix.runtime == 'browser' }}
        run: |
          node airship.js --use-links -f packages/Stability-Test install browser --target ${{ matrix.target }}

      - name: Run Browser Stability Test
        if: ${{ matrix.runtime == 'browser' }}
        run: |
          node packages/Stability-Test/dist/${{ matrix.target }}/browser-stability-test.js packages/Stability-Test/dist/${{ matrix.target }}/test.js
