package "flatlang/math"

class NumericTree {
    NumericOperation root

    public construct(String statement) {
        root = new NumericOperation(statement)
    }

    public toString() => "#root"
}