package flat/eventstream

class<Type> {
    public complete(ChannelReceiveResult<Type> result) {
        native var ChannelReceiveResult<Type> nativeResult

        nativeResult = result
        future.completed = true

        external {
            #{future.resolveFunc}(#{nativeResult});
        }
    }
}
