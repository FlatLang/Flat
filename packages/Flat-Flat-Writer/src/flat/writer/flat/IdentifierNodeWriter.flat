package flat/writer/flat

import flat/ast
import flat/compiler/models/Token
import flat/io
import flat/log/Logger
import flat/eventstream/EventStream
import flat/writer

class extends NodeWriter<IdentifierNode> {
    public write(IdentifierNode node, WriterOutputStream output) {
        output.writeToken(node.name)
    }
}
