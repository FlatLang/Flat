package flat/lexer

testable class {
    async test `incremental lexer matches whole input at every multibyte split`() {
        IncrementalLexerChecks().verifyEverySplit("let café = \"🙂 flat\"")
    }
}
