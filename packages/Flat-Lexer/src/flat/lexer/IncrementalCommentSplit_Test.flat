package flat/lexer

testable class {
    async test `incremental lexer matches whole input at every comment split`() {
        IncrementalLexerChecks().verifyEverySplit("one // comment\ntwo /* block\ncomment */ three")
    }
}
