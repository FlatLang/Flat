package flat/lexer

testable class {
    async test `incremental lexer matches whole input at every compound symbol split`() {
        IncrementalLexerChecks().verifyEverySplit("one <=> two >= three && four::five ?: six..seven")
    }
}
