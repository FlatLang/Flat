package flat/project

data class {
    visible String location
    visible Bool staticImport

    public construct(String location, Bool staticImport = false) {
        if (!location || location.trim().count == 0) throw Exception("Default import location cannot be empty")
        this.location = location
        this.staticImport = staticImport
    }
}
