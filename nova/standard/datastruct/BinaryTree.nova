package "nova/standard/datastruct"

public class BinaryTree<E> extends Tree<E>
{
	public BinaryNode<E> root
	{
		get
		{
			return super.root
		}
		
		set(BinaryNode node)
		{
			super.root = node
		}
	}
	
	public addNode(Object data) -> BinaryTree<E>
	{
		if (root == null)
		{
			root = new BinaryNode(data)
		}
		else
		{
			root.addChild(data)
		}
		
		return this
	}
}