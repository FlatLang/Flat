package "nova/math"

public class NumericStatement extends Statement
{
	NumericTree tree
	
	public construct(String statement)
	{
		tree = new NumericTree(statement.trim())
	}
	
	public toString() -> String
	{
		return tree.toString()
	}
}