package "stabilitytest"

import "nova/standard/time/Time";
import "nova/standard/thread/Thread";

public class TimeStability extends StabilityTestCase
{
	public test()
	{
		Console.write("Checking Time.nova... ")
		
		Long start = Time.currentTimeMillis()
		
		Thread.sleep(100)
		
		Long time = Time.currentTimeMillis() - start
		
		if (time >= 100 && time < 130)
		{
			Console.writeLine("OK")
		}
		else
		{
			program.fail("Failed; expected 100ms, found " + time + "ms")
		}
	}
}