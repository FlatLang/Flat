import "Thread"

public class ThreadImplementation extends Thread
{
	int times
	int millis
	
	public ThreadImplementation(int times, int millis)
	{
		this.times  = times
		this.millis = millis
	}
	
	public void run()
	{
		for (int i = 0; i < times; i++)
		{
			Thread.sleep(millis)
		}
	}
}